# raidwatch.env
# Copyright Roman Glos 2026
# RAID Watch - Konfiguration (DE/EN)
#
# DE: Diese Datei enthält nur Konfiguration. Keine Logik.
# EN: This file contains configuration only. No logic.

########################################
# Allgemein | General
########################################

# DE: Anzeigename des NAS im Betreff/Report (optional)
# EN: Display name of the NAS in subject/report (optional)
NAS_NAME="DH4300PLUS"

# DE: Sprache für Betreff + HTML-Report: de oder en
# EN: Language for subject + HTML report: de or en
REPORT_LANG="de"

# DE: Betreff-Prefix (Betreff wird automatisch auf max. 64 Zeichen gekürzt)
# EN: Subject prefix (subject is auto-trimmed to max 64 chars)
SUBJECT_PREFIX="[RAID]"

########################################
# SMTP / Mail
########################################

# DE: SMTP Server und Port
# EN: SMTP server and port
SMTP_SERVER="MAIL.UGREEN.COM"
SMTP_PORT="587"

# DE: SMTP Login (User/Pass). App-Passwort empfohlen.
# EN: SMTP auth (user/pass). App password recommended.
SMTP_USER="nas@UGREEN.COM"
SMTP_PASS="PASS"

# DE: Absender/Empfänger (kommagetrennt möglich)
# EN: From/To (comma-separated supported)
MAIL_FROM="nas@UGREEN.COM"
MAIL_TO="USER@UGREEN.COM"

# DE: TLS/SSL
# EN: TLS/SSL
# Port 587: STARTTLS=1, SSL=0 (typisch)
# Port 465: STARTTLS=0, SSL=1 (SMTPS)
SMTP_STARTTLS="1"
SMTP_SSL="0"
SMTP_TIMEOUT="20"

# DE: Mail-Format: html oder text
# EN: Mail format: html or text
MAIL_FORMAT="html"

# DE: HTML Detailsblock mit Roh-Logs anzeigen (1/0)
# EN: Show raw details block in HTML (1/0)
HTML_SHOW_DETAILS="1"

########################################
# Checks
########################################

# DE/EN: Aktivieren/Deaktivieren einzelner Checks (1/0)
CHECK_MDRAID="1"
CHECK_BTRFS="1"
# DE: Unter UGOS i.d.R. kein ZFS, daher Standard aus
# EN: Usually no ZFS on UGOS, so default off
CHECK_ZFS="0"

########################################
# Alarm-Verhalten | Alert behavior
########################################

# DE: Bei Statusänderungen auch ohne "Problem" mailen (z.B. Rebuild-Fortschritt)
# EN: Send mail on status changes even without a "problem" (e.g. rebuild progress)
ALERT_ON_CHANGE="0"

# DE: Cooldown (Sekunden). Verhindert Mail-Spam bei dauerhaftem Fehler.
# EN: Cooldown (seconds). Prevents mail spam on persistent issues.
ALERT_COOLDOWN_SECONDS="3600"

########################################
# Logging / State (wichtig für NVMe/eMMC/tmpfs)
########################################

# DE: Log-Verzeichnis. Empfehlung: auf NVMe/eMMC oder tmpfs legen,
#     nicht auf ein HDD-Volume, wenn du unnötige Spin-ups vermeiden willst.
# EN: Log directory. Recommended on NVMe/eMMC or tmpfs,
#     not on an HDD volume if you want to avoid unnecessary spin-ups.
# Standard: neben dem Script.
LOG_DIR=""

# DE: Optional separates Verzeichnis für State + Baymap.
#     Leer = identisch zu LOG_DIR.
# EN: Optional separate directory for state + baymap.
#     Empty = same as LOG_DIR.
STATE_DIR=""

# DE: Logs wie lange behalten (Tage). Alte Logs werden automatisch gelöscht.
# EN: Log retention in days. Old logs are auto-deleted.
LOG_RETENTION_DAYS="30"

# DE: Maximale Logdatei-Größe (MB) bevor rotiert wird.
# EN: Max log file size (MB) before rotation.
LOG_MAX_MB="5"

# DE: Anzahl Rotationsdateien (z.B. 3 = .1, .2, .3)
# EN: Number of rotated files to keep (e.g. 3 = .1, .2, .3)
LOG_ROTATE_COUNT="3"

# DE: Ruhige Läufe ohne Mail ins Log schreiben? 1 = ja, 0 = nein (empfohlen)
# EN: Log quiet runs without sending mail? 1 = yes, 0 = no (recommended)
LOG_NO_CHANGE_RUNS="0"

########################################
# Loop-Intervall (nur bei --loop)
########################################
SLEEP_SECONDS="60"
